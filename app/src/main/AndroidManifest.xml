<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.psm.projekt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_start"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_start_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewDialog"></activity>
        <receiver android:name=".Notification"></receiver>
        <activity android:name=".Update"></activity>
        <activity android:name=".MultiAdds"></activity>
        <activity android:name=".Multiadds_date"></activity>
    </application>

</manifest>